# Project-level environment example for orchestrators

# Backend (Django) database configuration
# If these are provided, backend uses PostgreSQL; else it falls back to SQLite.
DB_HOST=localhost
DB_PORT=5001
DB_NAME=emoji_ttt
DB_USER=postgres
DB_PASSWORD=postgres_password
