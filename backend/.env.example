# Django settings example environment variables

# Database configuration (PostgreSQL). If these are set, Django will use Postgres;
# otherwise it will fall back to SQLite (db.sqlite3 file).
DB_HOST=localhost
DB_PORT=5001
DB_NAME=emoji_ttt
DB_USER=postgres
DB_PASSWORD=postgres_password
