# Environment variables for the Django backend database configuration
# PostgreSQL will be used when these variables are provided; otherwise, the app falls back to SQLite.

# Hostname or IP address of the PostgreSQL server (e.g., database service/container name)
DB_HOST=localhost

# Port for PostgreSQL; this environment uses 5001 by default
DB_PORT=5001

# Database name
DB_NAME=emoji_ttt

# Database user
DB_USER=postgres

# Database user password
DB_PASSWORD=postgres_password

# Notes:
# - When DB_HOST, DB_NAME, DB_USER, and DB_PASSWORD are set, the backend uses Postgres.
# - If these variables are not provided, the backend will automatically fall back to SQLite at backend/db.sqlite3.
# - Do NOT commit an actual .env with secrets; this file is an example only.
